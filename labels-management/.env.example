# Path to a local checkout of laskoviymishka/lakekeeper on the labels-crud-verb branch.
# This is the build context for the patched Lakekeeper image.
LAKEKEEPER_SRC=../../lakekeeper

# Defaults for the demo warehouse (used by the notebook bootstrap).
WAREHOUSE_NAME=labels-demo
NAMESPACE=sales
TABLE=orders
